# Memoria.ia Server v0.1.0-alpha.9

- corrige a migração do catálogo para usar `PUT` no endpoint administrativo;
- impede que uma ativação com erro altere o perfil ativo;
- atualiza o catálogo após falhas;
- evita mensagens de erro duplicadas na interface;
- explica que outro perfil deve ser ativado antes da exclusão do atual.
